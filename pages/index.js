import ThemeSwitcher from "../components/ThemeSwitcher";
import Layout from "../components/Layout";

const Home = () => (
    <Layout>
        <ThemeSwitcher />
    </Layout>
);

export default Home;
